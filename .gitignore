# Auto-downloaded data (train.py fetches automatically)
data/

# Generated outputs (keep PNGs for project page, exclude log/npy)
results/*.log
results/*.npy

# Python cache
__pycache__/
*.pyc
*.pyo

# Optional / non-default model weights (not part of default ablation)
saved_models/gnn_*.pt
saved_models/informer_*.pt
saved_models/gated_*.pt

# Internal planning files (not for submission)
prepare.md
checklist.md
workflow.md
hparam_search.sh

# Report folder (submitted separately, not for public repo)
report/

# Assignment PDF (UCL copyright, not for redistribution)
*.pdf

# OS / editor artefacts
.DS_Store
*.swp
